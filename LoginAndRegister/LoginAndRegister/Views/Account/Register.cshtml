@using LoginAndRegister.Models
@model RegisterVM
@{
    ViewBag.Title = "Register";
}

<div class="container" style="margin-top:50px;">
    <div class="col-md-4 col-md-offset-4">
        <div class="panel panel-primary">
            <div class="panel-heading">
                Kayıt Paneli
            </div>
            <div class="panel-body">
                @using (Html.BeginForm("Register", "Account", FormMethod.Post))
                {
                    @Html.AntiForgeryToken()

                    @Html.DisplayNameFor(x => x.FirstName)
                    <div class="input-group">
                        @Html.TextBoxFor(x => x.FirstName, new { @placeholder = "İsminiz...", @class = "form-control" })
                        <div class="input-group-addon">
                            <i class="glyphicon glyphicon-lock"></i>
                        </div>
                    </div>
                    <br />
                    @Html.DisplayNameFor(x => x.LastName)
                    <div class="input-group">
                        @Html.TextBoxFor(x => x.LastName, new { @placeholder = "Soyisminiz...", @class = "form-control" })
                        <div class="input-group-addon">
                            <i class="glyphicon glyphicon-lock"></i>
                        </div>
                    </div>
                    <br />
                    @Html.DisplayNameFor(x => x.Email)
                    <div class="input-group">
                        @Html.TextBoxFor(x => x.Email, new { @placeholder = "E-Posta adresiniz...", @class = "form-control" })
                        <div class="input-group-addon">
                            <i class="glyphicon glyphicon-user"></i>
                        </div>
                    </div>
                    <br />
                    @Html.DisplayNameFor(x => x.Password)
                    <div class="input-group">
                        @Html.PasswordFor(x => x.Password, new { @placeholder = "Parolanız...", @class = "form-control" })
                        <div class="input-group-addon">
                            <i class="glyphicon glyphicon-lock"></i>
                        </div>
                    </div>
                    <br />
                    @Html.DisplayNameFor(x => x.PasswordVerify)
                    <div class="input-group">
                        @Html.PasswordFor(x => x.PasswordVerify, new { @placeholder = "Parolanız...", @class = "form-control" })
                        <div class="input-group-addon">
                            <i class="glyphicon glyphicon-lock"></i>
                        </div>
                    </div>
                    <br />
                    <div class="text-danger">
                        @Html.ValidationMessageFor(x => x.Email)
                    </div>
                    <div class="text-danger">
                        @Html.ValidationMessageFor(x => x.Password)
                    </div>
                    <input type="submit" class="btn btn-primary pull-right" value="Kayıt ol" />

                }
            </div>
            <div class="panel-footer">
                <div class="text-danger">
                    @Html.ValidationSummary()
                </div>
            </div>
        </div>
    </div>
</div>

